# 2026-04-20 工作记录

## 打包脚本与文档更新
- 创建了 `build.ps1` 一键打包 PowerShell 脚本，支持 Release/Dev 模式、自动清理、产物定位
- 更新了 `src-tauri/tauri.conf.json`，优化 bundle 配置：targets 改为 nsis+msi、添加 NSIS 安装程序配置（中文语言支持）
- 全面重写 `README.md`：添加功能介绍、快速开始指南、打包发布说明、项目结构、开发命令等完整文档
- 新增 `build.bat`：Windows 双击即执行的批处理入口，调用 build.ps1 完成打包（解决 .ps1 不能双击运行的问题）
