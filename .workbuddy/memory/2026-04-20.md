# 2026-04-20 ~ 2026-04-21 工作记录

## 打包脚本与文档更新
- 创建了 `build.ps1` 一键打包 PowerShell 脚本
- 更新了 `src-tauri/tauri.conf.json`：移除 Tauri 2.x 不合法的 nsis/macOS/linux bundle 属性（schema 校验报错）
- 全面重写 `README.md`
- 新增 `build.bat`：双击执行入口，带交互菜单选择打包类型
- **修复 build.ps1 多个 bug**：WriteHead 拼写错误导致花括号解析失败、编码问题改用纯英文输出
- **修复前端 TS 错误**：AppContent.vue 未使用变量 isMaxed 删除、trimEnd('/') 参数错误改用 replace 正则、ProviderForm.vue ?? 不可达改三元表达式
- **修复 Rust warning**：lib.rs isDark → is_dark 符合 snake_case 规范
- **新增便携版打包**：`-Portable` 模式直接复制 exe+dll 到文件夹，双击即用无需安装；`-SingleFile` 模式用 7z 打包成单 exe 自解压文件
- build.ps1 增加容错逻辑：NSIS 成功但 MSI 失败（如 WiX 下载超时）仍视为成功
