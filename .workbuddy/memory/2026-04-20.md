# 2026-04-20 工作日志

## 新增功能：快速配置（QuickSetup）组件
- **新增文件** `src/components/QuickSetup.vue` — 两步式向导组件，内置阿里云百炼（DashScope）预设模板
- **修改文件** `src/components/AppContent.vue` — 集成「⚡ 快速配置」按钮和 QuickSetup 组件
- **修改文件** `src/i18n/zh.ts` / `en.ts` — 补充 quick_setup 国际化文案

### 功能说明
用户点击「快速配置」按钮后：
1. **Step 1** 选择平台模板（目前内置阿里云百炼）
2. **Step 2** 只需填写 API Key + 选接口协议（OpenAI/Anthropic 兼容）+ 选默认模型 → 点击「一键生成配置」自动创建 Provider
- 百炼预设包含全部 9 个模型：qwen3.6-plus, qwen3.5-plus, qwen3-max-2026-01-23, qwen3-coder-next, qwen3-coder-plus, glm-5, glm-4.7, kimi-k2.5, MiniMax-M2.5
- 支持 OpenAI 兼容和 Anthropic 兼容两种 Base URL 切换
- 配置预览面板展示即将生成的完整信息

### 迭代改进（v2）
- **Anthropic 兼容改为默认选项**（适配 Claude Code / Cursor 等工具）
- **协议选择增加提示文字**：每个选项旁标注适用场景
- **选中后显示详细说明**
- **动态模型同步功能**：输入 API Key 后点击 🔄 按钮可从百炼 `/v1/models` 接口实时拉取最新模型列表
- 拉取失败时优雅降级到内置默认列表

### 迭代改进（v3）— 用户反馈修复
1. **平台卡片美化（参考 JCode 风格）**：
   - 彩色圆角图标容器，每张卡片有品牌色
   - hover 时放大 + 上浮动画效果
   - 扩充至 6 个平台模板：阿里云百炼、MiniMax、智谱 Zhipu、Kimi、火山引擎、腾讯云
2. **测试连通增加明确反馈**：
   - 点击后显示 loading toast：「正在测试「xxx」连通性...」
   - 成功 → `✅ 「xxx」连通正常`
   - 异常 → `⚠️ 响应异常（可能 Key 无效或网络不通）`
   - 失败 → `❌ 测试失败: xxx`
3. **切换 Provider 增加目标实例确认**：
   - 单实例模式：直接切换 + 显示成功 toast（含目标路径）
   - 多实例模式：弹出对话框确认注入目标（显示实例名和 config_dir 路径），用户确认后才执行切换
